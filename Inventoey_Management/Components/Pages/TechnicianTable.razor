@using CommunityToolkit.Maui.Storage
@using CsvHelper
@using CsvHelper.Configuration
@using System.Globalization
@using Microsoft.Maui.Storage
@using System.Text

<div class="d-flex justify-content-between">
    <div>
        <Button Color="ButtonColor.Info" class="btn btn-info mb-4" @onclick="OnShowImportModalClick">استيراد البيانات <Icon Name="IconName.Inbox" /></Button>
        <Button Color="ButtonColor.Info" class="btn btn-info mb-4" @onclick="PickFolder">تصدير البيانات <Icon Name="IconName.FileArrowUp" /></Button>
    </div>
</div>
<Grid TItem="Technician"
      Class="table table-hover table-bordered text-nowrap"
      DataProvider="CustomersDataProvider"
      AllowPaging="true"
      AllowSorting="true"
      AllowFiltering="true"
      Responsive="true"
      AllowRowClick="true"
      OnRowClick="r => navigate(r.Item.Id)"
      Unit="Unit.Px">

    <GridColumns>
        <GridColumn TItem="Technician" Freeze="true" FreezeLeftPosition="0" HeaderText="رقم العميل" PropertyName="Id" SortString="Id" SortKeySelector="item => item.Id" FilterTextboxWidth="100" HeaderTextAlignment="Alignment.Center" TextAlignment="Alignment.Center">
            @context.Id
        </GridColumn>
        <GridColumn TItem="Technician" HeaderText="التاريخ" PropertyName="UpdatedAt" SortString="UpdatedAt" SortKeySelector="item => item.UpdatedAt" FilterTextboxWidth="80" HeaderTextAlignment="Alignment.Center" TextAlignment="Alignment.Center">
            @context.UpdatedAt.Date
        </GridColumn>
        <GridColumn TItem="Technician" Filterable="false" HeaderText="أسم العميل" PropertyName="Name" SortString="Name" SortKeySelector="item => item.Name" FilterTextboxWidth="100" HeaderTextAlignment="Alignment.Center" TextAlignment="Alignment.Center">
            @context.Name
        </GridColumn>
        <GridColumn TItem="Technician" HeaderText="رقم التواصل" PropertyName="PhoneNumber" SortString="PhoneNumber" SortKeySelector="item => item.PhoneNumber" FilterTextboxWidth="80" HeaderTextAlignment="Alignment.Center" TextAlignment="Alignment.Center">
            @context.PhoneNumber
        </GridColumn>
    </GridColumns>
</Grid>

@* Modals *@
<Modal @ref="ImportModal" title="استيراد بيانات" IsVerticallyCentered="true">
    <BodyTemplate>
        حدد المستند المراد الاستيراد منه
        @if (!string.IsNullOrWhiteSpace(FileName))
        {
            <div class="mt-2">@FileName</div>
        }
        @if (isMathc)
        {
            <span class="text-danger">هناك عدد (@MatchRecord.Count) بيانات مطابقة في قاعدة البيانات</span>
            <div class="p-2 m-1">
                <Button Color="ButtonColor.Primary" @onclick="ImportRemoveDoublicate">تخطي البيانات المكررة</Button>
                <Button Color="ButtonColor.Danger" @onclick="ImportWithDoublicate">حفظ الكل</Button>
            </div>
        }
    </BodyTemplate>
    <FooterTemplate>
        <Button Color="ButtonColor.Secondary" @onclick="OnHideImportModalClick">إلغاء</Button>
        <Button Color="ButtonColor.Danger" @onclick="CheckData">استيراد</Button>
    </FooterTemplate>
</Modal>

<Modal @ref="ExportModal" title="تصدير بيانات" IsVerticallyCentered="true">
    <BodyTemplate>
        موقع الملف: @FolderPath
    </BodyTemplate>
    <FooterTemplate>
        <Button Color="ButtonColor.Secondary" @onclick="OnHideExportModalClick">إلغاء</Button>
        <Button Color="ButtonColor.Danger" @onclick="ExportData">تصدير</Button>
    </FooterTemplate>
</Modal>

@code {
    [Inject] public ITechnicianService TechnicianService { get; set; } = default!;
    [Inject] public NavigationManager NavigationManager { get; set; } = default!;
    private List<Technician> result = new();
    private IFolderPicker folderPicker = default!;
    private Modal ImportModal = default!;
    private Modal ExportModal = default!;
    private string FileName = string.Empty;
    private string FolderPath = string.Empty;
    private bool isMathc = false;
    private List<Technician> TechRecord = new List<Technician>();
    private List<Technician> MatchRecord = new List<Technician>();
    private ElementReference fileInput;
    private IEnumerable<Technician> data = Array.Empty<Technician>();

    private async Task<GridDataProviderResult<Technician>> CustomersDataProvider(GridDataProviderRequest<Technician> Technician)
    {
        string sortString = "";
        SortDirection sortDirection = SortDirection.None;

        if (Technician.Sorting is not null && Technician.Sorting.Any())
        {
            // Note: Multi column sorting is not supported at this moment
            sortString = Technician.Sorting.FirstOrDefault()!.SortString;
            sortDirection = Technician.Sorting.FirstOrDefault()!.SortDirection;
        }
        result = await TechnicianService.GetAllWithChildern();
        data = Technician.ApplyTo(result).Data ?? Enumerable.Empty<Technician>();
        StateHasChanged();
        return await Task.FromResult(Technician.ApplyTo(result));
    }

    private void navigate(int Id)
    {
        if (NavigationManager is not null)
        {
            NavigationManager.NavigateTo($"/technician/{Id}");
        }
    }

    private async Task CheckData()
    {
        var fileResult = await FilePicker.PickAsync(new PickOptions
        {
            PickerTitle = "Select technician CSV",
            FileTypes = new FilePickerFileType(new Dictionary<DevicePlatform, IEnumerable<string>>
            {
                { DevicePlatform.WinUI, new[] { ".csv" } },
            })
        });

        if (fileResult is null)
        {
            return;
        }

        FileName = fileResult.FullPath ?? fileResult.FileName;

        using (var stream = await fileResult.OpenReadAsync())
        using (var reader = new StreamReader(stream, Encoding.UTF8))
        using (var csv = new CsvReader(reader, CultureInfo.InvariantCulture))
        {
            TechRecord = csv.GetRecords<Technician>().ToList();
            MatchRecord = await TechnicianService.MatchRecords(TechRecord);
            if (MatchRecord.Count > 0)
            {
                // display the confirm overwrite or skip
                isMathc = true;
                StateHasChanged();
            }
            else
            {
                await ImportWithDoublicate();
                await ImportModal.HideAsync();
            }
        }
    }
    private async Task ImportWithDoublicate()
    {
        await TechnicianService.ImportData(TechRecord);
    }

    private async Task ImportRemoveDoublicate()
    {
        foreach (Technician technician in MatchRecord)
        {
            TechRecord.RemoveAll(t => t.Name == technician.Name);
        }
        await TechnicianService.ImportData(TechRecord);
        await ImportModal.HideAsync();
    }

    private async void ExportData()
    {
        string file = $"\\tech{DateTime.Now:yyyy-MM-dd_HH-mm-ss}.csv";
        string fullPath = string.Concat(FolderPath, file);
        using (var writer = new StreamWriter(fullPath, false, new UTF8Encoding(encoderShouldEmitUTF8Identifier: true)))
        using (var csv = new CsvWriter(writer, CultureInfo.InvariantCulture))
        {
            if (result.Count > 0) csv.WriteRecords(data);
        }
        await ExportModal.HideAsync();
    }

    private void OnFileSelected(ChangeEventArgs args)
    {
        FileName = args.Value?.ToString() ?? string.Empty;
        StateHasChanged();
    }
    private async Task OnShowImportModalClick()
    {
        await ImportModal.ShowAsync();
    }

    private async Task OnHideImportModalClick()
    {
        await ImportModal.HideAsync();
    }
    private async Task OnShowExportModalClick()
    {
        await ExportModal.ShowAsync();
    }

    private async Task OnHideExportModalClick()
    {
        await ExportModal.HideAsync();
    }

    private async Task PickFolder()
    {
        var result = await FolderPicker.Default.PickAsync();
        if (result.IsSuccessful)
        {
            FolderPath = result.Folder.Path;
            await ExportModal.ShowAsync();
        }
    }
}
