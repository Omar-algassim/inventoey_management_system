@page "/client/{Id:int}"

@using System.ComponentModel.DataAnnotations
@inject NavigationManager NavigationManager

<style>
    .valid.modified:not([type=checkbox]) {
        outline: 1px solid #26b050;
    }

    .invalid {
        outline: 1px solid red;
    }

    .validation-message {
        color: red;
    }
</style>
<h1 class="m-xxl-3">تعديل بيانات موظف</h1>
@if (client == null)
{
    <p>Loading...</p>
}
<EditForm Model="@client" OnValidSubmit="HandleOnValidSubmit">
    <DataAnnotationsValidator />
    <h3 class="mt-lg-5">معلومات الموظف</h3>

    <div class="container gap-3 border border-1 border-black rounded-3 p-3 m-4">
       <div class="row">
        <div class="form-group col mb-3">
            <label class="col-form-label w-100">اسم الموظف<span class="text-danger">*</span></label>
            <div>
                <InputText class="form-control" @bind-Value="client.Name" Placeholder="Enter Full name" />
                <ValidationMessage For="@(() => client.Name)" />
            </div>
        </div>

        <div class="form-group col mb-3">
            <label class="col-form-label w-100">رقم الموظف <span class="text-danger">*</span></label>
            <div>
                <InputText @bind-Value="client.ContactNumber" class="form-control" Placeholder="Enter spare code" />
                <ValidationMessage For="@(() => client.ContactNumber)" />
            </div>
        </div>
        <div class="form-group col mb-3">
            <label class="col-form-label w-100">رقم المبنى<span class="text-danger">*</span></label>
            <div>
                <InputNumber @bind-Value="client.BuildingNumber" class="form-control" Placeholder="Enter spare Amount" />
                <ValidationMessage For="@(() => client.BuildingNumber)" />
            </div>
        </div>
      </div>
    <div class="row">
        <div class="form-group col mb-3">
            <label class="col-form-label w-100">اسم المبنى<span class="text-danger">*</span></label>
            <div>
                <InputText @bind-Value="client.BuildingName" class="form-control" Placeholder="Enter spare Amount" />
                <ValidationMessage For="@(() => client.BuildingName)" />
            </div>
        </div>
            <div class="form-group col mb-3">
                <label class="col-form-label w-100">رقم المكتب<span class="text-danger">*</span></label>
                <div>
                    <InputText @bind-Value="client.OfficeNumber" class="form-control" Placeholder="Enter spare Amount" />
                    <ValidationMessage For="@(() => client.OfficeNumber)" />
                </div>
            </div>
        <div class="form-group col mb-3">
            <label class="col-form-label w-100">القسم<span class="text-danger">*</span></label>
            <div>
                <InputText @bind-Value="client.Department" class="form-control" Placeholder="Enter spare Amount" />
                <ValidationMessage For="@(() => client.Department)" />
            </div>
        </div>
      </div>
    </div>
    <div class="row">
        <div class="col-md-12 m-3 text-right d-flex w-auto justify-content-between">
            <Button Type="ButtonType.Button" Color="ButtonColor.Secondary" Class="float-end" @onclick="Cancel">إلغاء</Button>
            <Button Type="ButtonType.Submit" Color="ButtonColor.Success" Class="float-end me-2">حفظ</Button>
            <Button Type="ButtonType.Button" Color="ButtonColor.Danger" Class="float-end me-4" @onclick="OnShowModalClick">مسح</Button>
        </div>
    </div>
</EditForm>
<Modal @ref="modal" title="تأكيد" IsVerticallyCentered="true">
    <BodyTemplate>
        هل تريد فعلا حذف هذا العميل؟
        <Icon Name="IconName.Trash" Size="IconSize.x4" Color="IconColor.Danger" class="mr-5" />
    </BodyTemplate>
    <FooterTemplate>
        <Button Color="ButtonColor.Secondary" @onclick="OnHideModalClick">إلغاء</Button>
        <Button Color="ButtonColor.Danger" @onclick="DeleteClient">حذف</Button>
    </FooterTemplate>
</Modal>

@code {
    [Parameter]
    public int Id { get; set; }
    private Modal modal = default!;
    private Client client = new();
    private EditContext? editContext;

    [Inject] private ToastService ToastService { get; set; } = default!;
    [Inject] private IClientService clientService { get; set; } = default!;

    protected override async Task OnInitializedAsync()
    {
        client = await clientService.GetByIdAsync(Id);
    }


    public async void HandleOnValidSubmit()
    {
        try
        {
            await clientService.UpdateAsync(client.Id, client);
        }
        catch
        {
            var toastError = new ToastMessage
                {
                    Title = "حفظ بيانات الموظف",
                    Message = $"حدث خطأ اثناء الحفظ",
                    AutoHide = true,
                    Type = ToastType.Danger,
                    IconName = IconName.Bug,
                };
        }

        var toastMessage = new ToastMessage
        {
            Title = "حفظ بيانات الموظف",
            Message = $"تم حفظ بيانات الموظف بنجاح",
            AutoHide = true,
            Type = ToastType.Success,
            IconName = IconName.CheckCircleFill,
        };

        ToastService.Notify(toastMessage);
        NavigationManager.NavigateTo("/clients");
    }

    private void Cancel()
    {
        NavigationManager.NavigateTo("/clients");
    }
     private async Task OnShowModalClick()
    {
        await modal.ShowAsync();
    }

    private async Task OnHideModalClick()
    {
        await modal.HideAsync();
    }
    private async void DeleteClient()
    {
        try
        {
            await clientService.DeleteAsync(client);
        }
        catch
        {
            var toastError = new ToastMessage
            {
                Title = "حذف بيانات الموظف",
                Message = $"حدث خطأ اثناء الحذف",
                AutoHide = true,
                Type = ToastType.Danger,
                IconName = IconName.Bug,
            };
            ToastService.Notify(toastError);
            return;
        }
        var toastMessage = new ToastMessage
        {
            Title = "حذف بيانات الموظف",
            Message = $"تم حذف بيانات الموظف بنجاح",
            AutoHide = true,
            Type = ToastType.Success,
            IconName = IconName.CheckCircleFill,
        };
        ToastService.Notify(toastMessage);
        NavigationManager.NavigateTo("/clients");
    }
}
